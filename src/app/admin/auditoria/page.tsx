import { AuditoriaAdmin } from "@/components/admin/AuditoriaAdmin";

export const metadata = { title: "Auditoría · Administración | AndesMP" };

export default function AuditoriaAdminPage() {
  return <AuditoriaAdmin />;
}
